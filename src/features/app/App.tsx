import {ColoredUserPic} from '../imgContainer';

import s from './App.module.css';

export const App = () => {

  return (
    <div className={s.appContainer}>
      <ColoredUserPic/>
    </div>
  );
};
